# V1.4 audit increment — 2026-09-11

Reviewed baseline: `2b905df35747632bb408ff90b50e54cc9e6d87b3`, PR #212.
The supplied ChatGPT share page could not be fetched; the current repository and
PR description were used instead. This is a focused audit, not a complete
production security assessment or a Claude Code performance comparison.

## Findings and fixes

1. **Broken regression environment.** Hosted run 34543962108 passed 117 tests
   but failed the result migration contract because `/work/supabase/migrations`
   was missing. Supply migration files read-only and copy them into the temporary
   workspace. Include migration and Coding workspace changes in CI path triggers.
2. **Stale verification evidence.** Result projection could reuse a prior check
   event after a later edit or detected workspace mutation. Discard such evidence.
   Reject encrypted `verified` results unless all four checks passed without
   timeout on the final repair attempt.
3. **Navigation without evidence.** A zero-hit search previously proceeded to
   scope selection. Permit one bounded model-assisted query refinement, reject
   repeated queries, and stop if the refined search still finds nothing. This adds
   at most one provider call; existing zero-cost routing and validation apply.
   New-project/zero-match tasks can now stop conservatively; broader discovery
   needs a separately tested evidence path.

## Verification

All 124 tests in 17 targeted suites passed locally, including actual temporary
repository repair, encrypted results, job ownership/cancellation, UI, migrations,
and new search-refinement/stale-evidence regressions. Hosted verification must be
checked on the new commit; the earlier baseline's green general CI did not imply
a green Coding sandbox. Local typecheck completion could not be retrieved due to
a tool-session cancellation; use the new commit's CI as the remaining gate.

## Outstanding release gates

Real Postgres concurrency and migration execution; authenticated production
UI/API/worker/model/result/cancel smoke; recovery dispatch for stranded retryable
jobs; isolated Git delivery and deployment authorization; and held-out live-model
evaluation. The single-operator boundary is not multi-user authentication.
The worker must remain disabled until these deployment prerequisites are met.
No comparison with Claude Code has been measured, and this audit does not change
that status or enable paid fallback.
