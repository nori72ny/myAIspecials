# ORIGIN V1.4 Current Status — 2026-09-15

This file is the current operational snapshot for V1.4. It supplements `docs/ORIGIN-HANDOVER.md`. Stable product and safety policy remain governed by the canonical handover; this snapshot records live implementation and release evidence confirmed during the 2026-09-15 continuation work.

## Current main

- Repository: `nori72ny/myAIspecials`
- Current main SHA after PR #269: `201ee19523ddc404f3923c6cc146d362d3076d1e`
- Production project: `origin-personal`
- Production URL: `https://origin-personal.vercel.app`
- Vercel Git integration continues to auto-deploy main.
- The exact `201ee195...` Production deployment reached READY and owns the canonical production alias.

## V1.4 Coding status

Production Coding has already reached a full verified end-to-end state on recent main releases. Verified evidence includes authenticated Production smoke entry, exact-main SHA binding, durable hosted worker execution, encrypted result storage and owner-scoped retrieval, worker execution evidence, the expected synthetic file change, build/lint/test/typecheck verification, `gitPublished:false`, `deployed:false`, `freeOnly:true`, `costUsd:0`, and paid fallback disabled.

The durable Coding path is no longer classified as blocked by the earlier `CODING_JOB_AUTHENTICATION_REQUIRED` or `CODING_JOB_DISPATCH_UNAVAILABLE` deployment/configuration problems.

## Production continuity watch

PR #266 is merged. It added a low-frequency Production Coding continuity watch while preserving strict release gates.

- push-triggered Coding release gates remain strict
- scheduled checks run once per day at `03:17 UTC`
- GitHub OIDC remains restricted to the intended repository, main ref, fixed workflow, exact SHA, audience, and token lifetime
- only `push` and `schedule` are accepted event types
- a scheduled free-provider rate limit may be recorded as a skip
- the push release gate does not convert that condition into success
- no paid fallback is introduced

The schedule is monitoring evidence, not an uptime guarantee.

## Production alias convergence fix

After PR #266, Production smoke #26 passed the exact release health check and then briefly received `CODING_SMOKE_RELEASE_MISMATCH` on the smoke-start POST. Re-running the same main SHA succeeded. PR #267 was merged to absorb only this bounded Production alias convergence race.

- exact `/api/health` SHA gate is still required
- retry occurs only for HTTP 409 with exact code `CODING_SMOKE_RELEASE_MISMATCH`
- maximum six attempts, five seconds apart
- GitHub OIDC token is refreshed per retry
- every other start error remains fail-closed

## Required-tool truncation fix

On main `fb3e3906...`, Production smoke #27 successfully reached durable worker #28 but the free provider ended a required function-tool response before its arguments were complete. The job safely stopped as `blocked / CODING_PROVIDER_REQUIRED_TOOL_TRUNCATED`. This was not an authentication, dispatch, release-SHA, sandbox, or paid-routing failure.

PR #269 was implemented, fully gated, and merged as main `201ee19523ddc404f3923c6cc146d362d3076d1e`.

The fix is deliberately narrow:

- retry exactly one `PROVIDER_REQUIRED_TOOL_TRUNCATED` response across the entire Coding worker session
- retry the exact same trusted request, tool schema, provider/model plan, and environment
- never replay or repair partial tool arguments locally
- never relax schema validation
- never switch provider/model or enable paid fallback
- a second truncation fails closed
- all other provider failures bypass this retry and preserve their existing handling

Exact-head gates passed before merge: ACOS quality gate, lint, explicit typecheck, unit tests, API integration, production build/runtime checks, Production Release CI/CD, Chromium/Firefox/WebKit artifact isolation, Node 22/24 E2E and Lighthouse, CodeQL, OpenSSF Scorecard, Postgres 16/18 concurrency checks, bounded offline Coding session, and the Production worker isolation full suite.

The post-merge Production smoke #28 is running against exact main `201ee195...`; its durable worker is #29. This snapshot must only mark the truncation issue fully closed after that smoke reaches `verified`, not merely because the PR CI passed.

## Artifact Preview Isolation

Artifact preview isolation remains part of the production release gate. Chromium, Firefox, and WebKit isolation checks passed on the PR #269 exact head. Changes to preview capabilities must preserve the sandbox/security model and repeat browser-level verification.

## Answer and UI quality

PR #265 is merged and deployed. It added contextual answer-refinement controls for a more detailed answer, concrete examples, or a shorter summary.

The broader answer/UI objective remains active. Next work should improve task-specific depth, professional usefulness, long-answer hierarchy, code/evidence/failure presentation, representative mobile/tablet/desktop layouts, and held-out Agentic Coding evaluation. Do not use a global minimum answer length as a quality proxy, and do not claim Claude Code parity/superiority before benchmark evidence exists.

## Priority order from this snapshot

1. Require Production smoke #28 on exact main `201ee195...` to reach terminal `verified` and record the worker/result evidence.
2. Update this snapshot with the terminal smoke result and merge the documentation PR.
3. Refresh remaining stale live-status wording in the canonical handover/review document where needed.
4. Continue answer-quality and UI/UX improvements.
5. Implement the held-out Agentic Coding benchmark harness and release gates.
6. Only after benchmark evidence, evaluate comparative claims against Claude Code and other coding agents.

## Owner action

No owner action is currently required. Engineering, CI, deployment verification, and documentation remain on the operator side unless a future step requires an external account approval that cannot be performed through the connected tools.
