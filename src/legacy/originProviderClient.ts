import {
  ORIGIN_GOOGLE_AI_STUDIO_FREE_MODEL,
  ORIGIN_GROQ_FREE_MODEL,
  ORIGIN_OPENROUTER_FREE_MODEL,
  type OriginExecutionPlan,
  type OriginProviderDataPolicy,
} from "../lib/orchestration/OriginExecutionPolicy.js";
import { sanitizePreEgress, sanitizePreEgressPayload } from "../services/securitySanitizer.js";

export interface OriginChatMessage { role: "user" | "ai" | "assistant" | "model"; content: string; }
export interface OriginProviderExecutionRequest { plan: OriginExecutionPlan; messages: OriginChatMessage[]; systemInstruction: string; }
export interface OriginProviderRoutingEvidence { requestedModel: string; servedModel: string; strategy: string; provider: string; region?: string; attempt: 1; fallbackUsed: boolean; }
export interface OriginProviderExecutionResult { text: string; actualCostUsd: 0; providerDataPolicy: OriginProviderDataPolicy; routingEvidence: OriginProviderRoutingEvidence; usage: { promptTokens?: number; completionTokens?: number; totalTokens?: number; costUsd: 0; }; }
export type OriginProviderErrorCode = "PROVIDER_NOT_CONFIGURED" | "PROVIDER_POLICY_VIOLATION" | "PROVIDER_COST_UNVERIFIED" | "PROVIDER_ROUTING_UNVERIFIED" | "PROVIDER_RATE_LIMITED" | "PROVIDER_UNAVAILABLE" | "PROVIDER_TIMEOUT" | "PROVIDER_INVALID_RESPONSE" | "PROVIDER_INTERNAL_ERROR";
export interface OriginProviderDiagnostic { upstreamStatus?: number; upstreamErrorType?: string; transportFailure?: "timeout" | "network"; }
export class OriginProviderError extends Error { constructor(public readonly code: OriginProviderErrorCode, message: string, public readonly status: number, public readonly retryable: boolean, public readonly retryAfterSeconds?: number, public readonly diagnostic?: OriginProviderDiagnostic) { super(message); this.name = "OriginProviderError"; } }
export type OriginFetch = typeof fetch;
const MAX_COMPLETION_SEGMENTS = 3;
const SILENT_RETRY_DELAYS_MS = [200, 500, 1000] as const;
const PROVIDER_ATTEMPT_TIMEOUT_MS = 6000;
const PROVIDER_COOLDOWN_MS = 15_000;
export const ALLOWED_ZERO_COST_PROVIDERS = ["openrouter", "groq", "google-gemini"] as const;
export type AllowedZeroCostProvider = typeof ALLOWED_ZERO_COST_PROVIDERS[number];
export const ALLOWED_ZERO_COST_MODELS = { openrouter: [ORIGIN_OPENROUTER_FREE_MODEL] as const, groq: [ORIGIN_GROQ_FREE_MODEL] as const, "google-gemini": [ORIGIN_GOOGLE_AI_STUDIO_FREE_MODEL] as const } as const;
const PROVIDER_ID_BY_LABEL: Record<string, AllowedZeroCostProvider> = { OpenRouter: "openrouter", Groq: "groq", "Google AI Studio": "google-gemini", "google-ai-studio-free": "google-gemini", "openrouter-free": "openrouter", "groq-free": "groq" };
const providerCooldownUntil: Partial<Record<AllowedZeroCostProvider, number>> = {};
function nowMs(): number { return Date.now(); }
function markProviderCooldown(provider: AllowedZeroCostProvider): void { providerCooldownUntil[provider] = nowMs() + PROVIDER_COOLDOWN_MS; }
function isProviderCoolingDown(provider: AllowedZeroCostProvider): boolean { const until = providerCooldownUntil[provider] ?? 0; if (until <= nowMs()) { delete providerCooldownUntil[provider]; return false; } return true; }
function rejectZeroCost(message: string, code: OriginProviderErrorCode = "PROVIDER_POLICY_VIOLATION"): never { throw new OriginProviderError(code, message, 502, false); }
function assertFiniteZeroCost(value: unknown, field: string): asserts value is 0 { if (typeof value !== "number" || !Number.isFinite(value) || value < 0 || Math.abs(value) > Number.EPSILON) rejectZeroCost(`${field} が厳格な$0ポリシーを満たさないため、回答を破棄しました。`); }
function resolveAllowedProvider(provider: unknown): AllowedZeroCostProvider | null { if (typeof provider !== "string") return null; return PROVIDER_ID_BY_LABEL[provider] ?? (ALLOWED_ZERO_COST_PROVIDERS.includes(provider as AllowedZeroCostProvider) ? provider as AllowedZeroCostProvider : null); }
function isAllowedModel(provider: AllowedZeroCostProvider, model: unknown): model is string { if (typeof model !== "string") return false; if (provider === "openrouter" && !model.endsWith(":free")) return false; return (ALLOWED_ZERO_COST_MODELS[provider] as readonly string[]).includes(model); }
export function assertOriginZeroCostExecutionResult(result: OriginProviderExecutionResult, expectedModel?: string, expectedProvider?: string): void { if (!result || typeof result !== "object") rejectZeroCost("無料AIの実行結果を検証できないため、回答を返しません。", "PROVIDER_COST_UNVERIFIED"); assertFiniteZeroCost(result.actualCostUsd, "actualCostUsd"); assertFiniteZeroCost(result.usage?.costUsd, "usage.costUsd"); const e = result.routingEvidence; if (!e || typeof e !== "object") rejectZeroCost("プロバイダーの実行証跡が存在しないため、回答を返しません。", "PROVIDER_ROUTING_UNVERIFIED"); const provider = resolveAllowedProvider(e.provider); if (!provider) rejectZeroCost("許可されていないAIプロバイダーの実行証跡を検出したため、回答を返しません。", "PROVIDER_ROUTING_UNVERIFIED"); if (typeof e.requestedModel !== "string" || typeof e.servedModel !== "string") rejectZeroCost("要求モデルまたは提供モデルを確認できないため、回答を返しません。", "PROVIDER_ROUTING_UNVERIFIED"); if (!isAllowedModel(provider, e.servedModel)) rejectZeroCost("許可されていない無料モデル、または無料モデルとして確認できないモデルを検出したため、回答を返しません。", "PROVIDER_ROUTING_UNVERIFIED"); if (expectedProvider && resolveAllowedProvider(expectedProvider) !== provider) rejectZeroCost("選択Providerと実行Providerが一致しないため、回答を返しません。", "PROVIDER_ROUTING_UNVERIFIED"); if (expectedModel && e.requestedModel !== expectedModel) rejectZeroCost("選択モデルと実行モデルが一致しないため、回答を返しません。", "PROVIDER_ROUTING_UNVERIFIED"); if (e.attempt !== 1) rejectZeroCost("未許可の実行試行番号を検出したため、回答を返しません。", "PROVIDER_ROUTING_UNVERIFIED"); if (e.fallbackUsed && e.strategy !== "zero-cost-failover") rejectZeroCost("フォールバック実行の戦略を確認できないため、回答を返しません。", "PROVIDER_ROUTING_UNVERIFIED"); }
export function originCompletionTokenBudget(taskType: OriginExecutionPlan["taskType"]): number { switch (taskType) { case "implementation": case "documentation": return 2400; case "research": case "review": case "architecture": case "security": case "current-information": return 1800; default: return 1200; } }
function normalizeMessages(messages: OriginChatMessage[], systemInstruction: string) { return [{ role: "system", content: sanitizePreEgress(systemInstruction) }, ...messages.map((m) => ({ role: m.role === "ai" || m.role === "assistant" || m.role === "model" ? "assistant" : "user", content: sanitizePreEgress(m.content) }))]; }
function extractText(content: unknown): string { if (typeof content === "string") return content.trim(); if (!Array.isArray(content)) return ""; return content.filter((p): p is { type?: string; text?: string } => typeof p === "object" && p !== null).filter((p) => p.type === "text" && typeof p.text === "string").map((p) => p.text!.trim()).filter(Boolean).join("\n\n").trim(); }
function mergeContinuation(previous: string, continuation: string): string { const left = previous.trimEnd(); const right = continuation.trimStart(); for (let n = Math.min(left.length, right.length, 500); n >= 20; n -= 1) if (left.slice(-n) === right.slice(0, n)) return `${left}${right.slice(n)}`.trim(); return `${left}\n\n${right}`.trim(); }
function parseRetryAfterSeconds(v: string | null): number | undefined { if (!v) return undefined; const n = Number(v); if (Number.isFinite(n) && n > 0) return Math.ceil(n); const t = Date.parse(v); return Number.isFinite(t) ? Math.max(1, Math.ceil((t - Date.now()) / 1000)) : undefined; }
function mapHttpFailure(status: number, retryAfterSeconds?: number): OriginProviderError { const d = { upstreamStatus: status }; if (status === 401) return new OriginProviderError("PROVIDER_NOT_CONFIGURED", "無料AIの認証情報を確認できませんでした。", 401, false, undefined, d); if (status === 402) return new OriginProviderError("PROVIDER_POLICY_VIOLATION", "無料モデルの利用に支払いが必要と判定されたため、実行を停止しました。", 502, false, undefined, d); if (status === 403 || status === 404) return new OriginProviderError("PROVIDER_UNAVAILABLE", "無料AIを現在利用できません。", 503, true, undefined, d); if (status === 429) return new OriginProviderError("PROVIDER_RATE_LIMITED", retryAfterSeconds ? `無料AIの利用上限に達しました。約${retryAfterSeconds}秒後に再試行できます。` : "無料AIの利用上限に達しました。時間をおいて再試行してください。", 429, true, retryAfterSeconds, d); if (status === 408 || status === 504) return new OriginProviderError("PROVIDER_TIMEOUT", "無料AIの応答が時間内に完了しませんでした。", 504, true, undefined, d); if (status === 502 || status === 503) return new OriginProviderError("PROVIDER_UNAVAILABLE", "無料AIを現在利用できません。", 503, true, undefined, d); return new OriginProviderError("PROVIDER_INTERNAL_ERROR", "無料AIの処理に失敗しました。", status >= 400 && status < 600 ? status : 500, status >= 500, undefined, d); }
function validatePlan(plan: OriginExecutionPlan): void { if (!plan.freeOnly || plan.estimatedCostUsd !== 0 || plan.providerDataPolicy.allowProviderFallbacks !== true || plan.providerDataPolicy.dataCollection !== "deny" || plan.providerDataPolicy.requireZeroDataRetention !== false) throw new OriginProviderError("PROVIDER_POLICY_VIOLATION", "0ドル固定ポリシーに適合しない実行計画は使用できません。", 400, false); if (!["openrouter-free", "google-ai-studio-free", "groq-free"].includes(plan.providerId)) throw new OriginProviderError("PROVIDER_POLICY_VIOLATION", "許可されていないプロバイダーです。", 400, false); }
function freeUsage(cost: unknown): 0 { assertFiniteZeroCost(cost, "usage.costUsd"); return 0; }
async function readJson(response: Response): Promise<Record<string, any>> { try { return await response.json() as Record<string, any>; } catch { throw new OriginProviderError("PROVIDER_INVALID_RESPONSE", "無料AIから有効な応答を受け取れませんでした。", 502, true); } }
function isRetryableHttpStatus(status: number): boolean { return status === 408 || status === 429 || status === 500 || status === 502 || status === 503 || status === 504; }
async function fetchWithSilentRetry(fetchImpl: OriginFetch, input: RequestInfo | URL, init: RequestInit): Promise<Response> { let lastError: unknown; for (let attempt = 0; attempt <= SILENT_RETRY_DELAYS_MS.length; attempt += 1) { if (attempt > 0) await new Promise((resolve) => setTimeout(resolve, SILENT_RETRY_DELAYS_MS[attempt - 1])); const controller = new AbortController(); const timeout = setTimeout(() => controller.abort(), PROVIDER_ATTEMPT_TIMEOUT_MS); try { const response = await fetchImpl(input, { ...init, signal: controller.signal }); if (response.status === 429) throw mapHttpFailure(429, parseRetryAfterSeconds(response.headers.get("Retry-After"))); if (!isRetryableHttpStatus(response.status) || attempt === SILENT_RETRY_DELAYS_MS.length) return response; lastError = mapHttpFailure(response.status, parseRetryAfterSeconds(response.headers.get("Retry-After"))); } catch (error) { if (error instanceof OriginProviderError) throw error; lastError = error; if (attempt === SILENT_RETRY_DELAYS_MS.length) break; } finally { clearTimeout(timeout); } } throw new OriginProviderError("PROVIDER_TIMEOUT", "無料AIの応答が時間内に完了しませんでした。", 504, true, undefined, { transportFailure: "timeout" }); }
